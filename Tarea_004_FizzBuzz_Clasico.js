/**
 * Tarea_004_FizzBuzz_Clasico
 *
 * ---CONCEPTOS A APLICAR---
 *
 * - Bucle `for`: Una estructura de control que nos permite repetir un bloque
 *   de código un número determinado de veces. Su sintaxis clásica es
 *   `for (inicialización; condición; expresión_final)`. Es ideal para
 *   iterar sobre un rango de números.
 *
 * - Lógica condicional (`if`, `else if`, `else`): Permite encadenar múltiples
 *   comprobaciones. El orden es crucial: se debe comprobar primero la
 *   condición más específica (múltiplo de 3 y 5) para evitar que las
 *   condiciones más generales (múltiplo de 3 o múltiplo de 5) se ejecuten
 *   antes de tiempo.
 *
 * - Operador de módulo (`%`): Nuevamente, es esencial para determinar si un
 *   número es múltiplo de otro. Si `numero % divisor === 0`, entonces
 *   `numero` es múltiplo de `divisor`.
 *
 * ---INSTRUCCIONES---
 *
 * Objetivo: Combinar bucles y lógica condicional para resolver un problema
 * de programación clásico, prestando atención al orden de las comprobaciones.
 *
 * 1. Escribe un programa que recorra los números del 1 al 100.
 *
 * 2. Para los múltiplos de 3, imprime "Fizz" en lugar del número.
 *
 * 3. Para los múltiplos de 5, imprime "Buzz" en lugar del número.
 *
 * 4. Para los números que son múltiplos de tanto 3 como 5, imprime "FizzBuzz".
 *
 * 5. Para todos los demás números, simplemente imprime el número.
 *
 * ---SALIDA ESPERADA (fragmento)---
 * 1
 * 2
 * Fizz
 * 4
 * Buzz
 * Fizz
 * ...
 * 14
 * FizzBuzz
 * ...
 */

// Escribe tu código aquí debajo.
