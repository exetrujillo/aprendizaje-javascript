/**
 * Tarea_007_Trabajando_con_Objetos
 *
 * ---CONCEPTOS A APLICAR---
 *
 * - Creación de Objetos Literales (`{}`): Un objeto es una colección de pares
 *   clave-valor. Es la estructura ideal para representar una entidad única
 *   (como un usuario, un producto, un coche) que tiene múltiples propiedades
 *   o características.
 *
 * - Sintaxis de Clave-Valor (`clave: valor`): Dentro de un objeto, cada
 *   propiedad se define con un nombre (la clave, que es un string) seguido
 *   de dos puntos y su valor. Las propiedades se separan por comas.
 *
 * - Acceso a Propiedades (Notación de Punto): Para leer el valor de una
 *   propiedad de un objeto, se utiliza la sintaxis `nombreDelObjeto.nombreDeLaPropiedad`.
 *
 * - Pasar Objetos como Argumentos: Las funciones pueden recibir objetos
 *   completos como argumentos. Esto permite trabajar con toda la entidad
 *   dentro de la función, haciendo el código más organizado.
 *
 * ---INSTRUCCIONES---
 *
 * Objetivo: Aprender a agrupar datos relacionados en una sola entidad (un objeto)
 * y a manipular dichos datos a través de funciones.
 *
 * 1. Crea una constante llamada `usuario` y asígnale un objeto literal.
 *
 * 2. El objeto debe tener al menos tres propiedades:
 *    - `nombre`: un string (ej: "Tulio")
 *    - `edad`: un número (ej: 42)
 *    - `pais`: un string (ej: "Chipre")
 *
 * 3. Define una función llamada `describirUsuario`. Esta función debe aceptar
 *    un solo parámetro, que será el objeto de usuario.
 *
 * 4. Dentro de la función, accede a las propiedades del objeto recibido para
 *    construir y `return` un string con el siguiente formato:
 *    "Tulio, de 42 años, vive en Chipre."
 *
 * 5. Fuera de la función, llama a `describirUsuario` pasándole tu objeto `usuario`.
 *
 * 6. Guarda la descripción devuelta en una constante e imprímela en la consola.
 *
 */

// Escribe tu código aquí debajo.