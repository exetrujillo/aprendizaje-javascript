/**
 * Tarea_003_Par_o_Impar
 *
 * ---CONCEPTOS A APLICAR---
 *
 * - Operador de módulo/resto (`%`): Esta es la herramienta matemática clave
 *   para este problema. El operador `%` devuelve el *resto* de una división
 *   entera. Por ejemplo, `10 % 2` es `0` (porque 10 es divisible por 2 sin
 *   sobrante), mientras que `7 % 2` es `1`. Un número es par si el resto de
 *   dividirlo por 2 es exactamente 0.
 *
 * - Lógica condicional (`if...else`): Es la estructura fundamental en
 *   programación para ejecutar diferentes bloques de código según si una
 *   condición es verdadera (`true`) o falsa (`false`).
 *
 * - Operador de igualdad estricta (`===`): Compara si dos valores son
 *   idénticos tanto en valor como en tipo. Es la forma más segura de
 *   comparar en JavaScript, ya que evita conversiones de tipo inesperadas
 *   (a diferencia de `==`).
 *
 * ---INSTRUCCIONES---
 *
 * Objetivo: Aplicar lógica condicional para tomar decisiones basadas en
 * el valor de una variable.
 *
 * 1. Crea una variable (puedes usar `let` si quieres poder cambiarla
 *    fácilmente para probar) llamada `numero` y asígnale un número entero.
 *
 * 2. Escribe la lógica para determinar si `numero` es par o impar.
 *
 * 3. Si es par, imprime en consola un mensaje como "El número 10 es par".
 *
 * 4. Si es impar, imprime en consola un mensaje como "El número 7 es impar".
 *
 * [OPCIONAL]: Intenta resolver el problema usando un operador ternario
 * en lugar de un `if...else` para una solución más compacta.
 *
 */

// Escribe tu código aquí debajo.