/**
 * Tarea_005_Creando_Funciones
 *
 * ---CONCEPTOS A APLICAR---
 *
 * - Declaración de funciones (`function nombreFuncion(parametro) { ... }`):
 *   Es la sintaxis para definir un bloque de código reutilizable. Las funciones
 *   son uno de los pilares fundamentales de la programación, ya que nos
 *   permiten encapsular lógica, darle un nombre y ejecutarla cuando queramos.
 *
 * - Parámetros y Argumentos: Un `parámetro` (ej: `nombre`) es la variable que
 *   se lista en la definición de la función. Un `argumento` (ej: "Juan") es el
 *   valor real que se pasa a la función cuando se la invoca.
 *
 * - `return`: La palabra clave que finaliza la ejecución de una función y
 *   especifica un valor para ser devuelto al código que la llamó. Una función
 *   sin un `return` explícito devuelve `undefined` por defecto. Separar la
 *   creación de un valor (dentro de la función) de su uso (fuera de la función)
 *   es un principio clave de un buen diseño de software.
 *
 * ---INSTRUCCIONES---
 *
 * Objetivo: Abstraer una lógica repetitiva en una función reutilizable para
 * promover un código más limpio y modular (Principio DRY: Don't Repeat Yourself).
 *
 * 1. Define una función llamada `crearSaludo`.
 *
 * 2. La función debe aceptar un parámetro llamado `nombre`.
 *
 * 3. Dentro de la función, construye el string de saludo (ej: "Hola, Juan").
 *
 * 4. La función no debe imprimir nada en la consola. En su lugar, debe
 *    DEVOLVER el string que creó usando la palabra clave `return`.
 *
 * 5. Fuera de la función, llama a `crearSaludo` pasándole un nombre de tu elección.
 *
 * 6. Guarda el resultado (el string devuelto) en una nueva constante llamada `saludoFinal`.
 *
 * 7. Finalmente, imprime la constante `saludoFinal` en la consola.
 *
 */

// Escribe tu código aquí debajo.
