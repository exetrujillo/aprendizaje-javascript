/**
 * Tarea_001_Hola_Mundo
 *
 * ---INSTRUCCIONES---
 *
 * Objetivo: Familiarizarse con el entorno de ejecución de Node.js y el comando
 * fundamental para visualizar la salida de un programa.
 *
 * 1. Escribe una única línea de código que imprima el texto exacto 
 *    "¡Hola, Mundo!" en la consola o terminal.
 *
 * ---CONCEPTOS A APLICAR---
 *
 * - `console.log()`: El método `log` del objeto global `console`.
 *   En el entorno de Node.js, `console` es un objeto global que nos proporciona
 *   una interfaz para interactuar con la terminal (la salida estándar o `stdout`).
 *   Lo usamos para depurar, mostrar resultados y, en general, entender qué
 *   está ocurriendo durante la ejecución de nuestro código. Es la ventana
 *   principal al estado interno de nuestra aplicación.
 *
 */

// Escribe tu código aquí debajo.