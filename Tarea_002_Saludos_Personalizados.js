/**
 * Tarea_002_Saludos_Personalizados
 *
 * ---CONCEPTOS A APLICAR---
 *
 * - `const`: Una declaración de variable cuyo valor (o más precisamente,
 *   cuya referencia) no puede ser reasignado. Usar `const` por defecto
 *   hace nuestro código más predecible y nos protege de cambios accidentales.
 *
 * - `Template Literals` (Plantillas de cadena): Una forma moderna y legible
 *   de incrustar expresiones (como variables) dentro de un string. Se definen
 *   con comillas invertidas (``) y las expresiones se insertan con `${expresion}`.
 *   Son preferibles a la concatenación tradicional con `+` por su claridad.
 *
 * ---INSTRUCCIONES---
 *
 * Objetivo: Introducir el concepto de variables para almacenar y reutilizar
 * datos, haciendo nuestro código más dinámico y legible.
 *
 * 1. Declara una variable de tipo constante llamada `nombre` y asígnale
 *    un valor de tipo string (ej: "Ana").
 *
 * 2. Utiliza esta constante para construir e imprimir un saludo personalizado
 *    en la consola. El resultado debe ser "Hola, Ana" (o el nombre que
 *    hayas elegido).
 *
 */

// Escribe tu código aquí debajo.